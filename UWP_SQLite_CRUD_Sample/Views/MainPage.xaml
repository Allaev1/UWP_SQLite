<Page
    x:Class="UWP_SQLite_CRUD_Sample.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_SQLite_CRUD_Sample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dataBase="using:DataBase" 
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <AppBarButton HorizontalAlignment="Right" Icon="Add" Command="{x:Bind ViewModel.AddCommand}"/>
        <ListBox ItemsSource="{x:Bind ViewModel.Products}" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate x:DataType="dataBase:Product">
                    <TextBlock>
                        <Run Text="{x:Bind Id}"/>
                        <Run Text="). "/>
                        <Run Text="{x:Bind ProductName}"/>
                        <Run Text=" - "/>
                        <Run Text="{x:Bind UnitPrice}"/>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
